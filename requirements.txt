Django>=4.2
djangorestframework
python-dotenv
psycopg2-binary  # if you plan to use Postgres; for local dev sqlite works without this
requests
openai   # optional, only if you will use OpenAI
transformers[sentencepiece]  # optional: for local emotion model if you plan to use HF
torch>=1.13.0  # if using local HF models (optional)
